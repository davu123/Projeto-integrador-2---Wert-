# Sprint 9 — F10, F16, F21 (US10, US16, US21)

**Quase tudo novo.** A referência não tem `PUT /equipamentos/:id` nem rota/motorista no PDF.

## Implementar

1. **F10** — `PUT /equipamentos/:id` + página ou modo edição na listagem (não usar `newequipment.jsx`).
2. **F16** — `gerarRelatorio` inclui rota e motorista **quando existirem** (lote ligado a parada/rota).
3. **F21** — dashboard ou lista do gestor: coletas feitas vs pendentes a partir do status da sprint 8.

## Não fazer

GPS, atribuição automática, reabrir escopo do BB.

## Aceite da sprint

Editar marca/modelo de um equipamento já lançado; PDF de um lote com rota atribuída cita motorista; gestor vê no sistema se a coleta foi recolhida sem WhatsApp.
